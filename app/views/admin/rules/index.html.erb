<% @page_title = '管理画面：詰め合わせ設定' %>

<div class="page-header">
  <h1>詰め合わせ設定</h1>
</div>

<% if @rules.present? %>
<div class="table-responsive">
  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th class="text-center">詰め合わせ禁止カテゴリ</th>
        <th class="text-center"></th>
      </tr>
    </thead>
    <tbody>
      <% @rules.each do |rule| %>
      <tr>
        <td class="col-md-2"><span class="label label-primary"><%= rule.category1_id %></span></td>
        <td class="text-center col-md-2">
          <%= link_to '詳細', admin_rule_path(rule), class: 'btn btn-info btn-xs',
            style: 'width: 4em' %>
          <%= link_to '削除', admin_rule_path(rule), method: :delete, class: 'btn btn-xs btn-danger',
            style: 'width: 4em;', data: { disable_with: '削除中'} %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= paginate @rules %>
<% else %>
<p>詰め合わせ設定がありません。</p>
<% end %>
